interface:
  display_name: "AlphaSift"
  short_description: "Screen, score, and rank A-share stock ideas"
  default_prompt: "Use $alphasift to list strategies or run an A-share screening strategy."

policy:
  allow_implicit_invocation: true
